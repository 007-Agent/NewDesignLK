import React from "react";
import ReactDOM from "react-dom/client";
import PasswordPage from "./Password/PasswordPage/PasswordPage";
import "./App.css";
import "./index.css";
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PasswordPage />
  </React.StrictMode>,
);
